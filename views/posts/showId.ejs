<!DOCTYPE html>
<html>
<head>
    <title><%= post.title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    <h1><%= post.title %></h1>
    <p><%= post.body %></p>
    <% if (post.attachment) { %>
        <img src="/posts/<%= post.id %>/attachment" alt="Post image">
    <% } %>
</body>
</html>